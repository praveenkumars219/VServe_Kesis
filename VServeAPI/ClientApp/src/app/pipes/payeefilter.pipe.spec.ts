import { PayeefilterPipe } from './payeefilter.pipe';

describe('PayeefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PayeefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
